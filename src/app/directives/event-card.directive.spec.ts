import { EventCardDirective } from './event-card.directive';

describe('EventCardDirective', () => {
  it('should create an instance', () => {
    const directive = new EventCardDirective();
    expect(directive).toBeTruthy();
  });
});
