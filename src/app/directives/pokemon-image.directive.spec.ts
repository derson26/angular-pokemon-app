import { PokemonImageDirective } from './pokemon-image.directive';

describe('PokemonImageDirective', () => {
  it('should create an instance', () => {
    const directive = new PokemonImageDirective();
    expect(directive).toBeTruthy();
  });
});
